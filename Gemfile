source 'https://rubygems.org'

gem 'rails', git: 'git://github.com/rails/rails.git' # Needed for Rails 5.0
gem 'arel',  git: 'git://github.com/rails/arel.git'  # Needed for Rails 5.0
gem 'rack',  git: 'git://github.com/rack/rack.git'   # Needed for Rails 5.0
gem 'sprockets',  git: 'git://github.com/rails/sprockets.git' # Needed for Rails 5.0
gem 'sass-rails', git: 'git://github.com/rails/sass-rails.git' # Needed for Rails 5.0
gem 'sprockets-rails', '3.0.0.beta2' # Needed for Rails 5.0

gem 'sqlite3'
gem 'overcommit'

group :development do
  gem 'dawnscanner', require: false
end
